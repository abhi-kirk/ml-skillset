**Check Asteroid conditions**
- Check new asteroid conditions in a `while` loop. 
- Attach `else` to the while loop for when no condition applies. 
- Time Complexity: $O(n)$. 
- Space Complexity: $O(1)$. 